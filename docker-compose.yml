services:
  loan-tracker:
    build: .
    ports:
      - "8086:8080"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_PATH=/app/data/loan_tracker.db
      - TZ=America/Toronto
    restart: unless-stopped
